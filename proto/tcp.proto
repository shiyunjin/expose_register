syntax = "proto3";

package Tcp;

option go_package = "internal/proto/tcp;protoc";

message Req {
  int64 status = 1;
  bytes data = 2;
}

message Resp {
  string token = 1;
  bytes data = 2;
}

service TCP {
  rpc Connect (stream Resp) returns (stream Req) {}
}
